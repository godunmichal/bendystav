import React from 'react'
import { HeroContainer, HeroTitle } from './HeroElements'

function Hero() {
    return (
        <HeroContainer>
            <HeroTitle>Renovace a výstavba</HeroTitle>
        </HeroContainer>
    )
}

export default Hero
